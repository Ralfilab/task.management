{
  "version": "2.0.0",
  "tasks": [
    {
      "label": "Run API Server",
      "type": "shell",
      "command": "dotnet",
      "args": [
        "run",
        "--project",
        "Api.Server/Api.Server.csproj"
      ],
      "options": {
        "cwd": "${workspaceFolder}"
      },
      "problemMatcher": []
    }
  ]
}
